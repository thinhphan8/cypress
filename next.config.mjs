/** @type {import('next').NextConfig} */
const nextConfig = {
    // Server Actions are available by default now, `experimental.serverActions` option can be safely removed.
    // experimental: {
    //     serverActions: true
    // },
    images: {
        domains: ['mnleaqfggpkawuruazmx.supabase.co'],
    },
};

export default nextConfig;
